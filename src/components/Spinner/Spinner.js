import React from 'react';
import './Spinner.css'

const Spinner = () => {
    return (
        <div>
            <h1 className='load'>Loading...</h1>
        </div>
    );
};

export default Spinner;